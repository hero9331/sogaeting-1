<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.temiboardgame">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TemiBoardGame"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:allowBackup"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Temi SDK Metadata -->
        <meta-data
            android:name="com.robotemi.sdk.metadata.SKILL"
            android:value="true" />
        <meta-data
            android:name="com.robotemi.sdk.metadata.KIOSK"
            android:value="true" />

        <!-- ✅ 새로 추가된 화면 등록 -->
        <activity android:name=".CongratsActivity" />

        <activity android:name=".ResultActivity" />
        <activity android:name=".TileActivity" />
        <activity android:name=".BonusMoveActivity" />
        <activity android:name=".IslandActivity" />
        <activity android:name=".EyeGameActivity" />

        <activity android:name=".LightReactionGameActivity" />
        <activity android:name=".PressureGameActivity" />
        <activity android:name=".TimeGameActivity" />
        <activity android:name=".PockyGameActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
